#include <stdio.h>
#include <stdbool.h>
#include <memory.h>
#include <chrono>
#include <Windows.h>
#include <thread>
#include <stdint.h>

#define PI (3.141592653f)

#define MAX(a, b) (((a) > (b)) ? (a) : (b))
#define MIN(a, b) (((a) < (b)) ? (a) : (b))
