# 2d-ascii-shader

Convert a single shader animation frame video into a single ASCII image representation of it.

<https://www.youtube.com/watch?v=xNX9H_ZkfNE>

```bash
cd build
cmake ..
cmake --build . --config Release&&.\Release\cpu-rasterizer.exe
```
